# ClassIRFier.jl
ClassIRFier.jl is a neural-network-based tool designed to classify shapes of Impulse Response Functions (IRFs). Its main purpose is to support sign-restricted SVAR identification by providing an automated and consistent IRF pattern classifier that can be embedded directly into the model’s selection process.
